import 'package:flutter/material.dart';

class SettingsAlarmScreen extends StatelessWidget {
  const SettingsAlarmScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
