import 'package:flutter/material.dart';

class SettingsBlockListScreen extends StatelessWidget {
  const SettingsBlockListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
