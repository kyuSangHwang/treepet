import 'package:flutter/material.dart';

class SettingsEventListScreen extends StatelessWidget {
  const SettingsEventListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
