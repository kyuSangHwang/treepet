import 'package:flutter/material.dart';

class SettingsLogOutScreen extends StatelessWidget {
  const SettingsLogOutScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
