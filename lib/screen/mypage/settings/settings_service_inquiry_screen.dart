import 'package:flutter/material.dart';

class SettingsServiceInquiryScreen extends StatelessWidget {
  const SettingsServiceInquiryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
