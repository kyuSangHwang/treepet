import 'package:flutter/material.dart';

class SettingsTermOfUserScreen extends StatelessWidget {
  const SettingsTermOfUserScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
