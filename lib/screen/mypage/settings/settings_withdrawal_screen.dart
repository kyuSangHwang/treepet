import 'package:flutter/material.dart';

class SettingsWithdrawalScreen extends StatelessWidget {
  const SettingsWithdrawalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
